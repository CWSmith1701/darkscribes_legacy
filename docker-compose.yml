version: 3

services:
  web:
    image: nginx:alpine
    restart: always
    volumes:
      - 
  php:
    image: php

  database:
    image: mariadb
    volumes:
      - data/db:var/lib/mysql
  dbadmin:
    image: phpmyadmin
    restart: always
    environment:
    depends_on:
      database    
